node_modules
.DS_Store
dist
dist-ssr
*.local
# .env is intentionally committed (encrypted by dotenvx).
# .env.keys contains the private decryption key — never commit it.
.env.keys
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.claude/settings.local.json
